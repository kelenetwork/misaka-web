services:
  misaka-web:
    image: kelework/misaka-web:latest
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    ports:
      - "127.0.0.1:3000:3000"
